'use client';

import styled from '@emotion/styled';

const Text = styled.p`
  font-size: 0.65rem;
  color: rgba(255, 255, 255, 0.5);
  letter-spacing: 0.06em;
  text-align: center;
  margin-top: 40px;
`;

export default function FooterText() {
  return (
    <Text>2.0.2.6 COMPUTER SCIENCE DEPARTMENT MT</Text>
  );
}
